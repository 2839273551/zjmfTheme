# 当前状态与后续计划

> 状态基线：2026-08-21。本页记录已知事实；真实线上状态仍需结合目标服务器和最新发布记录核对。

## 已有覆盖

| 界面 | 已有 `codex_framework` 内容 | 仍需验证或完善 |
| --- | --- | --- |
| Web | 首页、公共样式、Cart 共用头尾 | 公告、文档、关于等内容页 |
| Cart | 商品分类、列表、配置外壳、实时订单摘要 | 查看购物车、结算、支付、完成页 |
| Client Area | 公共头尾、动态侧栏、认证页、仪表盘、服务列表与详情外壳 | 账单、工单、资料、安全、实名认证及多种产品详情分支 |

已重点检查过登录页、服务列表和经典 `dcimcloud` 服务详情的桌面与移动布局，但这不代表 Cloud/DCIM v10、VNC、SSL 和所有产品分支均已完成。

## 极验插件状态

`geetest_captcha` 1.0.2 已于 2026-08-21 发布到生产，运行时代码基线为 GitHub 提交 `ac80650b588f579caaf03d138935342ff9e25cd1`，版本标签为 `geetest-captcha-v1.0.2`。

本次发布新增 44px 整行行为验证入口、桌面与移动响应式状态样式，以及点击、Enter 和空格键调用插件现有 `showCaptcha()` 的触发方式。登录页已确认加载 `geetest-captcha.js?v=1.0.2`，桌面和手机均无横向溢出或控制台错误，真实 GT4 滑块弹层可以正常唤起。

生产发布记录：

- 备份：`/root/codex_backups/geetest-captcha-1.0.2-20260821-141635/`。
- 暂存：`/root/codex_uploads/geetest-captcha-1.0.2-20260821-141635/`。
- 运行文件：`GeetestCaptchaPlugin.php`、`assets/geetest-captcha.js`、`clientarea/codex_framework/custom.css`。
- 线上 SHA-256：`0664316866d3837606092049511ed13fc628cd3b821d0252fcdfeca8f72394e9`、`1f1927660c10eabaf5c3aa456b9c490a580ff6dc0258fd90e9843cdb43458e07`、`5e2a6fb147c304ac096fd96ddfef5f4d316798e9a6a5a47c0c094c3e1a1daecf`。
- 三个文件均为 `www:www 0644`，缓存清理返回 `Clear Successed`。

本次验收没有拖动或提交真实验证码，因此服务端成功验证、失败关闭、登录提交、修改密码 AJAX 注入仍需使用测试账号完成闭环验证。生产后台当前关闭注册页和密码页的极验开关，所以 `/register`、`/pwreset` 和 `/security` 未出现极验入口；不得把这些分支标记为已验证。

## 后续顺序

1. 修复并真实验证极验插件服务端拦截链路。
2. 验证服务列表变体和普通服务详情分支。
3. 审计 Cloud/DCIM v10、VNC 和高风险控制动作。
4. 依次覆盖账单、工单、资料、安全和实名认证页面。
5. 验证登录态加车、优惠码、结算和支付入口。
6. 完成 Web 内容页、主题预览图和最终品牌定稿。

各页面族、购物闭环、质量保障和发布治理的完整任务清单见[开发路线图](开发路线图.md)。

## 完成定义

- 真实路由和真实数据验证通过。
- 桌面、平板、手机均无阻断问题。
- 服务端业务契约未被视觉重构破坏。
- 代码、文档和发布记录引用同一 GitHub 提交。
- 存在明确、已验证的回滚路径。
