# IDC 项目 Codex 工作指令

## 唯一权威来源

- 本项目的唯一权威仓库是 `https://github.com/2839273551/zjmfTheme.git`，基准分支为 `main`。
- 仓库内的 `README.md`、`文档/` 和当前代码是有效资料；不要依赖或重新创建仓库外的独立文档副本。

## 每个新任务的启动检查

在对本项目执行实质工作前：

1. 确认当前 Git 远端仍指向上述仓库。
2. 对 `origin/main` 执行有超时限制的 `git fetch`，比较本地 `HEAD`、`origin/main` 和工作区状态。
3. 若本地工作区干净、当前分支仅落后远端，使用 `git merge --ff-only origin/main` 快进同步。
4. 若存在未提交修改、分支分叉或本地领先，不得执行重置、强制拉取或覆盖；先保留本地内容并说明差异。仍应使用 `git show origin/main:README.md` 和 `git show origin/main:文档/<文件>` 读取 GitHub 最新文档。
5. 读取 `README.md` 以及与当前任务有关的 `文档/` 文件后再实施修改。若网络不可用，应明确说明本次只能依据最后一次已获取的 `origin/main`，不得谎称已在线核对。

Windows 系统代理启用且 GitHub 直连失败时，可仅对当前 Git 命令使用 `http://127.0.0.1:7897`；不要把代理、凭据或令牌持久写入仓库。

## 开发与安全边界

- 默认主题开发范围仅限三个 `服务器源码/public/themes/*/codex_framework/` 目录；扩大范围前先核对任务需要。
- 不提交真实数据库配置、`.env`、密码、密钥、令牌、证书、服务器配置、运行缓存、备份或用户上传资料。
- 不修改 ionCube 加密核心或 `vendor/` 中的第三方实现，除非用户明确要求且风险已核实。
- 保留现有干净灰蓝视觉基线，不恢复已废弃的深炭色/暖信号方案。
- 发布生产环境时只发布本任务实际变更：先清单、只读检查和备份，再暂存、校验 SHA-256、逐文件安装并清缓存；禁止整站覆盖、远端递归删除、`rsync --delete` 或递归权限修改。

## 文档维护

- 文档变更直接修改仓库内 `README.md` 或 `文档/`，与代码一起核对并推送到 GitHub；不再保留独立副本。
- 完成推送后核对本地 `HEAD`、`origin/main` 与 GitHub 远端提交，并确认重要文档可从 GitHub 打开。
