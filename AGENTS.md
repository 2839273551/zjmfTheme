# IDC 本地长期记忆启动指令

## 每个任务开始

1. 从仓库根目录读取相邻 Vault 的 `..\Obsidian记忆库\00-总索引.md`。
2. 按总索引匹配 IDC，只读取 `..\Obsidian记忆库\10-项目\IDC模板.md`；该页是长期规则、关键决策、发布约定和历史经验的权威来源。
3. 执行有超时的 `git fetch origin main`，比较当前分支、本地 `HEAD`、`origin/main` 和 `git status --short`。
4. 获取完成后读取本次任务目标端的当前代码。源码、`VERSION`、缓存参数和文件存在性以最新 Git 状态为准。

如果相邻 Vault 或 IDC 项目页不存在，向用户询问一次路径并停止实质工作。不得从旧聊天、已删除的仓库文档或迁移归档静默恢复规则。

## 权威来源边界

- 当前用户指令优先于所有长期规则。
- 本地 IDC 项目页负责长期规则；GitHub `main` 负责当前源码、版本和公开项目说明。
- README 是公开项目概览，不复制完整 Agent 规则。
- 工作区已有内容不得重置、覆盖或偷偷混入当前任务。
- 推送时只提交本任务文件，推送后核对本地 `HEAD`、`origin/main` 和 GitHub 远端提交一致。
